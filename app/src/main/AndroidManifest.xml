<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.coffeetime">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CoffeeTime">
        <activity android:name=".UserQRActivity"></activity>
        <activity
            android:name=".Menu_Navegation"
            android:label="@string/app_name"
            android:theme="@style/Theme.CoffeeTime.NoActionBar" />
        <activity android:name=".StartAppActivity" />
        <activity android:name=".auth.SignUp" />
        <activity android:name=".auth.SignIn" />
        <activity
            android:name=".SplashScreenActivity"
            android:label="@string/title_activity_menu_navegation"
            android:theme="@style/Theme.CoffeeTime.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" />
    </application>
</manifest>